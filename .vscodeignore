# Source & build tooling (only dist/ + media/ runtime assets ship)
src/**
tests/**
scripts/**
esbuild.js
tsconfig.json
dist/test.cjs
dist/test-aggregate.cjs
**/*.map
**/*.ts

# Editor / repo / local tooling
.vscode/**
.github/**
.gitignore
.vscodeignore
.claude-flow/**
.remember/**

# Docs & screenshots (README references images by absolute URL, no need to ship them)
docs/**

# Artifacts
*.vsix
node_modules/**
